# Captures hold a live session (logon scramble, screen data) — never commit.
captures/*.jsonl
captures/*.log
*.jsonl
# Built binaries.
/tap
/lens
/server
/C
odgp-server
/tui

# throwaway decode/scratch programs (agents + manual)
zz_*.go
*_tmp.go

# research-agent scratch decoders
scratch_*

# Local MCP server configs hold live SAP credentials — never commit.
.mcp.json
